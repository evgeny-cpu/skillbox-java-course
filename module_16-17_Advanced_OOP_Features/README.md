# Lambda выражения и Stream API в Java

Этот репозиторий содержит решения практических задач по работе с функциональными интерфейсами, лямбда-выражениями и Stream API в Java.

## 📚 Содержание

1. **Сортировка сотрудников** — лямбда-выражения и компараторы.
2. **Поиск сотрудника с максимальной зарплатой** — Stream API.
3. **Поиск рейсов в аэропорту** — комплексное использование Stream API.

---

## 👨‍💼 Задача 1: Сортировка сотрудников (7.2 Домашняя работа 7.1)

**Цель:** Освоить лямбда-выражения для сортировки коллекций.

### Условие

Необходимо отсортировать список сотрудников **одновременно** по двум критериям:
1. По заработной плате (по возрастанию или убыванию).
2. По алфавиту (имя или фамилия).

### Решение

Использован компаратор с `thenComparing()` для многоуровневой сортировки:

```java
List<Employee> sortedEmployees = employees.stream()
    .sorted(Comparator
        .comparing(Employee::getSalary)
        .thenComparing(Employee::getName))
    .collect(Collectors.toList());